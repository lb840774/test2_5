collection_name = "lr-serial-pipeline-collection"

# Create collection if needed
try:
    sm.create_model_package_group_collection(
        ModelPackageGroupCollectionName=collection_name,
        ModelPackageGroupCollectionDescription="Serial inference pipeline collection"
    )
    print("Collection created")
except sm.exceptions.ResourceInUse:
    print("Collection already exists")

# Add groups to collection
for group_name in [g1, g2, g3]:
    try:
        sm.create_model_package_group_collection_membership(
            ModelPackageGroupCollectionName=collection_name,
            ModelPackageGroupName=group_name
        )
        print(f"Added {group_name} to collection")
    except sm.exceptions.ResourceInUse:
        print(f"{group_name} already in collection")
